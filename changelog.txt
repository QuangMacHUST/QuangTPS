# QuangTPS Changelog

## Ngày 24/11/2024

### Sửa lỗi
1. Sửa lỗi trong `quangtps/dicom/pacs_client.py`:
   - Thay thế `VerificationServiceClass.service_class` bằng UID trực tiếp '1.2.840.10008.1.1'

2. Sửa lỗi trong `quangtps/ui/patient_tab.py`:
   - Đổi `full_name_field` thành `name_edit` ở dòng kết nối tín hiệu vì biến full_name_field không tồn tại

3. Sửa lỗi trong `quangtps/dose/algorithms/monte_carlo.py`:
   - Sửa lỗi indentation trong phần tạo energy spectrum

4. Sửa lỗi trong `quangtps/treatment/treatment_delivery.py`:
   - Sửa lỗi indentation trong hàm update_fraction_status

### Cài đặt công cụ
- Cài đặt autopep8 để tự động định dạng mã nguồn

## Ngày 25/11/2024

### Thêm biểu tượng cho UI
1. Tạo các biểu tượng SVG cho giao diện:
   - app_icon.svg - Biểu tượng chính của ứng dụng
   - patient.svg - Biểu tượng quản lý bệnh nhân
   - planning.svg - Biểu tượng lập kế hoạch
   - dose.svg - Biểu tượng tính toán liều
   - treatment.svg - Biểu tượng điều trị
   - qa.svg - Biểu tượng QA
   - report.svg - Biểu tượng báo cáo

2. Cập nhật MainWindow để sử dụng các biểu tượng mới

### Phát triển mô hình dữ liệu
1. Phát triển các lớp cơ sở:
   - Patient - Lưu trữ thông tin bệnh nhân
   - BeamParameters - Thông số chùm tia
   - Beam - Mô hình chùm tia điều trị
   - Plan - Kế hoạch điều trị

### Môi trường
1. Tạo script thiết lập môi trường:
   - setup.py - Script thiết lập cơ bản
   - setup_environment.py - Script thiết lập đầy đủ

### Sửa lỗi
1. Sửa lỗi trong `quangtps/ui/main_window.py`:
   - Thêm ICON_DIR để chuẩn hóa đường dẫn biểu tượng

### Thay đổi tiếp theo sẽ được thêm vào đây... 

## [2025-03-24]
### Fixed
- Sửa lỗi encoding tiếng Việt trong logging bằng cách thêm tham số encoding='utf-8' cho FileHandler
- Đảm bảo cài đặt các gói phụ thuộc cần thiết: scikit-image và dicompyler-core
- Sửa lỗi kiểm tra gói phụ thuộc trong run_quangtps.py: ánh xạ tên gói với tên module chính xác (scikit-image → skimage, dicompyler-core → dicompylercore)
- Sửa lỗi trong hàm show_splash_screen(): đảm bảo luôn trả về cùng một kiểu dữ liệu (app, splash) trong mọi trường hợp
- Cải thiện chẩn đoán lỗi: thêm traceback đầy đủ trong log khi khởi động ứng dụng gặp lỗi 

## [2025-03-26]
### Fixed
- Sửa lỗi định dạng (indentation) trong file quangtps/treatment/treatment_delivery.py, hàm update_fraction_status() để đảm bảo tính nhất quán của mã nguồn
- Đảm bảo mã nguồn tuân thủ PEP 8 để tránh lỗi indentation và cải thiện khả năng đọc
- Sửa lỗi thiếu import trong quangtps/core/types.py: thêm `from enum import Enum` để sử dụng Enum class
- Sửa lỗi trong quangtps/treatment/treatment_delivery.py: chuyển class DeliveryStatus thành lớp kế thừa đúng từ Enum để phù hợp với thiết kế chung của codebase
- Sửa lỗi indentation trong quangtps/dose/algorithms/monte_carlo.py: khắc phục lỗi thụt lề không đúng trong phần else
- Sửa lỗi 'break' outside loop trong quangtps/dose/algorithms/monte_carlo.py: đặt câu lệnh break trong phạm vi vòng lặp hoặc thay thế bằng pass
- Thêm hàm connection() thiếu trong quangtps/database/db_connector.py để trả về kết nối đến cơ sở dữ liệu
- Cải thiện hàm connection() bằng cách triển khai context manager để có thể sử dụng với câu lệnh with
- Sửa hàm _create_tables() trong PatientDatabase để sử dụng trực tiếp kết nối từ DBConnector thay vì sử dụng with statement
- Thống nhất việc sử dụng biến self.conn thay vì self.connection trong DBConnector để tránh nhầm lẫn
- Thêm các hàm callback thiếu trong quangtps/imaging/volume_renderer.py: _preset_changed(), _opacity_changed(), _window_level_changed() và các hàm liên quan 